# ===================================
# Application Configuration
# ===================================
NODE_ENV=development
PORT=3000
LOG_LEVEL=info

# ===================================
# Database Configuration
# ===================================
# For local development (with docker-compose):
DATABASE_URL="postgresql://galacash:galacash123@localhost:5433/galacash_db"

# For production (Cloud SQL):
# DATABASE_URL="postgresql://username:password@host:5432/database"

# ===================================
# Redis Configuration
# ===================================
# For local development:
REDIS_URL=redis://localhost:6379

# For production (Cloud Memorystore):
# REDIS_URL=redis://your-redis-host:6379

# ===================================
# JWT Configuration
# ===================================
# Generate secure random strings for production:
# openssl rand -base64 64
JWT_SECRET=your-super-secret-jwt-access-token-key-change-this-in-production
JWT_REFRESH_SECRET=your-super-secret-jwt-refresh-token-key-change-this-in-production

# ===================================
# GCP Cloud Storage Configuration
# ===================================
# Leave empty to disable file uploads (will show warning)
GCP_PROJECT_ID=
GCP_BUCKET_NAME=galacash-bucket

# Path to service account credentials JSON file
# Download from GCP Console > IAM > Service Accounts
GOOGLE_APPLICATION_CREDENTIALS=

# ===================================
# Scheduled Jobs Configuration
# ===================================
# Cron expression for monthly bill generation
# Default: "0 0 1 * *" (runs at midnight on the 1st of every month)
# Format: "minute hour day month dayOfWeek"
BILL_GENERATION_SCHEDULE=0 0 1 * *

# ===================================
# CORS Configuration
# ===================================
# Allowed origins for CORS (comma-separated for multiple origins)
# Use "*" for development, specific domains for production
CORS_ORIGIN=*

# ===================================
# Notes
# ===================================
# 1. Never commit .env file to version control
# 2. Copy this file to .env and fill in your values
# 3. For production, use environment variables from your hosting platform
# 4. Generate secure JWT secrets using: openssl rand -base64 64
